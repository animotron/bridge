the theme-concrete-root-layout
    (root-layout, theme-concrete)
    (\h1 get title)
    (\p get content)
    (\ul
        (\li ("Host: ") (\strong get host))
        (\li ("URI: ")  (\strong get uri)))
