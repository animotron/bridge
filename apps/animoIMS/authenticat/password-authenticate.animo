the password-authenticate
	(\form
		(@id "password-authenticat")
		(@class "k-widget")
		(\fieldset
			(\legend "authentication")
			(\p (\label "login" (\input (@name "login") (@required))))
			(\p (\label "password" (\input (@name "password") (@required))))
			(\p \button "authenticate")
		)
	)
