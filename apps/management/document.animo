def document
	
	(event)
	
	(word 
		(lang-en "document")
		(lang-ru "документ")
	)
	
	(\div (@id "document")
		(\label "Номер" (\input (@id "document-number") (@disabled)))
		(\label "от" (\input (@id "document-date") (@value now)))
	)