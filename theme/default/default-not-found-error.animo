the default-not-found-error
    (html-page)
    (use default, error)
    (title "Not found")
    (message
        ("The server has not found anything matching the request ")
        (\strong get uri) ("."))

